import { Megaphone, PenLine, Send, Users } from "lucide-react";
import { Reveal } from "./Reveal";
const steps=[[PenLine,"Write your offer","Create a short promotional message with your offer, discount or announcement."],[Users,"Choose your audience","Select a customer group or upload the contacts you want to reach."],[Send,"Launch campaign","Send your promotional SMS and follow the campaign delivery status."]] as const;
export function HowItWorks(){return <section id="how-it-works" className="how-section"><div className="container section"><Reveal className="section-intro dark-intro"><span className="dark-kicker"><Megaphone size={14}/> Promotional SMS</span><h2>Three steps. Then your offer is out.</h2><p>No complicated setup. Create your promotion, choose your customers and send.</p></Reveal><ol className="steps-grid">{steps.map(([Icon,title,body],i)=><Reveal as="li" key={title} delay={i*90}><div className="step-card"><div className="step-top"><span className="step-icon"><Icon size={20}/></span><span>Step {i+1}</span></div><h3>{title}</h3><p>{body}</p></div></Reveal>)}</ol></div></section>}
